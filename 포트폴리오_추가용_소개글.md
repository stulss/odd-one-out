# 포트폴리오 추가용 소개글

> **이 파일의 용도**: 개인 포트폴리오를 작업하는 다른 대화창에 이 파일 내용을 그대로 붙여넣으면,
> 이 미니게임 프로젝트를 포트폴리오에 한 항목으로 추가할 수 있습니다.
>
> ⚠️ **배포 주소는 아직 빈칸입니다.** GitHub Pages에 올린 뒤 `(배포 주소)` 부분을 채워서 쓰세요.

---

## 1. 프로젝트 요약 (사람이 읽는 버전)

**딱 하나 이상함** — 여러 개 중 딱 하나 다른 것을 찾는 브라우저 관찰력 게임

- **한 줄 소개**: 격자에 놓인 비슷한 도형 중 딱 하나만 미세하게 다릅니다. 제한 시간 안에 그것을 찾는 게임으로, 프레임워크도 이미지도 음원도 없이 순수 HTML/CSS/JavaScript만으로 만들었습니다.
- **사용 기술**: Vanilla JavaScript (ES Modules), CSS `clip-path`, HTML5 Canvas 2D, Web Audio API, localStorage, Web Share API
- **핵심 기능 3가지**
  1. **서버 없이 "같은 링크 = 같은 문제"** — 시드 난수(xmur3 + mulberry32)로 스테이지를 생성해, 링크를 받은 사람이 어떤 기기에서 열어도 완전히 동일한 문제를 봅니다. 날짜는 KST로 고정해 해외에서 열어도 같은 '오늘의 문제'가 나옵니다. 서버·DB·로그인이 하나도 없습니다.
  2. **이미지 0개, 차이는 코드로 제어** — 게임 오브젝트를 전부 CSS로 그립니다. 이 게임의 재미는 "미묘한 차이"인데, 차이를 1px·3° 단위로 정확히 제어해야 난이도 곡선이 성립합니다. 래스터 이미지는 축소 과정에서 차이가 뭉개져 불공정한 실패를 만들기 때문에 벡터/CSS를 택했습니다. 차이 축은 색조·밝기·크기·회전·모서리·투명도·위치·기울기·채도·두께 10종이고, 각 축에 **지각 임계값**을 두어 "아무도 못 찾는 판"이 나오지 않게 했습니다.
  3. **공유 카드에 문제를 넣기** — 결과 카드(1080×1350)를 Canvas로 생성하는데, 점수만 넣으면 자랑으로 끝납니다. **마지막 문제의 격자를 정답 표시 없이** 넣어서, 카드를 본 사람이 카드 자체를 풀게 되고 링크를 누를 이유가 생기도록 설계했습니다.
- **안정성**: rAF 루프가 앱 전체에 1개뿐, DOM 노드를 재사용해 스테이지 전환 시 생성/삭제 0, 1초에 10회 연타해도 진행은 1단계만, 일시정지 중 남은 시간 오차 0.05초 미만. 저장값이 비었거나 깨져 있어도(6가지 케이스) 기본값으로 실행됩니다.
- **접근성**: 마우스 없이 키보드만으로 완주 가능, 음소거·움직임 줄이기 선택 제공. 움직임 줄이기는 효과를 없애는 대신 색 점멸로 대체해 "맞았는지 틀렸는지"는 계속 알 수 있게 했습니다.
- **크기**: 소스 전체 80KB, 외부 라이브러리 0개
- **배포 주소**: (배포 주소)
- **소스 코드**: (저장소 주소)

### 이 프로젝트에서 얻은 것

가장 크게 배운 건 **자동 검증을 의심하는 법**이었습니다. 세 번이나 "초록불"이 아무것도 증명하지 않고 있었습니다. 가로 넘침 검사는 CSS에 `overflow-x:hidden`이 걸려 있어 어떤 경우에도 통과했고, 스크린샷 스크립트는 12장이 전부 "연결할 수 없음" 오류 화면인데도 파일 크기만 보고 전부 `OK`를 찍었으며, 촬영 도구는 지정한 해상도를 무시하고 더 넓게 그린 뒤 잘라내고 있었습니다. 이후로는 검증 코드를 짤 때 **"이 검사가 실패하는 경우를 하나 만들어볼 수 있나"** 를 먼저 확인하게 됐습니다.

또 하나는 **결함은 발견 시점에 따라 가격이 다르다**는 것이었습니다. 격자 칸 수를 화면 크기에 맞춰 조절하도록 만들었더니 같은 시드라도 기기마다 다른 문제가 나왔고, 이건 '오늘의 문제'와 '도전장' 기능을 통째로 무너뜨리는 결함이었습니다. 프로토타입 단계에서 찾아 코드 3줄로 끝났지만, 공유 기능을 다 만든 뒤였다면 기능 하나를 다시 설계해야 했을 겁니다.

---

## 2. 포트폴리오 데이터 형식 (붙여넣기용)

### 2-1. 강점 카드 형식 (상황 / 행동 / 결과)

```javascript
{
  id: 'odd-one-out',
  title: '초록불을 의심하는 힘',
  summary: '관찰력 미니게임을 만들며 통과하던 검사가 사실 아무것도 검사하지 않고 있음을 찾아냈다',
  situation:
    '격자에서 다른 하나를 찾는 브라우저 게임을 프레임워크 없이 만들면서, ' +
    '두 기준 해상도에서 화면이 잘리지 않는지 자동으로 검사하도록 만들어 두었다.',
  action:
    '검사는 계속 "넘침 없음"으로 통과했는데 스크린샷에서는 게임판이 잘려 보였다. ' +
    '검사 코드를 다시 보니 documentElement.scrollWidth로 재고 있었고, ' +
    'CSS에 overflow-x:hidden이 걸려 있어 넘친 내용이 잘려나가므로 ' +
    '어떤 경우에도 false를 반환하는, 실패할 수 없는 검사였다. ' +
    '게임판의 실제 좌표를 뷰포트 경계와 직접 비교하는 방식으로 바꿨다.',
  result:
    '바꾼 검사로 11스테이지 전 구간을 다시 재서 실제 좌표(16~374px / 뷰포트 390px)를 ' +
    '근거로 확보했다. 같은 종류의 문제를 두 건 더 찾았다 — 오류 화면 12장을 전부 ' +
    '성공으로 집계하던 스크린샷 스크립트, 지정한 해상도를 무시하던 촬영 도구. ' +
    '이후 자동 검사에는 "실패했다면 어떤 흔적이 남는가"를 반드시 넣게 됐다.',
  evidence: {
    label: '게임 플레이해보기',
    url: '(배포 주소)',
  },
}
```

### 2-2. 트러블슈팅 형식 (문제 / 시도 / 비교 / 배운 점)

자세한 내용은 [트러블슈팅.md](트러블슈팅.md)에 6건 전부 정리해 두었습니다. 그중 대표 2건입니다.

```javascript
{
  id: 'seed-grid-mismatch',
  problem:
    '"같은 링크를 연 사람은 같은 문제를 본다"가 이 게임의 핵심 기능인데, ' +
    '격자 칸 수를 화면 크기에 맞춰 조절하도록 만들어 둔 탓에 ' +
    '1280px에서는 48칸, 375px에서는 24칸이 나왔다. 칸 수가 다르면 정답 위치도 ' +
    '달라지므로, 같은 시드라도 기기마다 다른 문제가 나오고 있었다.',
  attempt:
    '원인은 최소 터치 크기 44px를 지키려고 넣은 "화면이 좁으면 칸을 줄인다"는 ' +
    '규칙이었다. 조작성을 위한 판단이었지만 시드 재현성을 통째로 깨뜨렸다. ' +
    '격자를 스테이지 번호로만 결정하도록 바꾸고, 상한을 360px 기기에서도 ' +
    '44px가 보장되는 4×6=24칸으로 고정했다. 난이도는 칸 수 대신 ' +
    '차이의 미묘함으로만 올리도록 했다.',
  comparison:
    '수정 전에는 같은 시드로 뽑은 25스테이지가 기기마다 달랐고, ' +
    '수정 후에는 25스테이지 전부 완전히 일치한다. 수정 규모는 코드 3줄이었다.',
  lesson:
    '프로토타입 단계라 3줄로 끝났지만, 공유 기능을 다 만든 뒤 발견했다면 ' +
    '기능 하나를 통째로 다시 설계해야 했다. 되돌리기 비싼 결함일수록 ' +
    '일찍 찾아야 한다는 걸 손해를 보기 전에 배웠다.',
},
{
  id: 'green-light-illusion',
  problem:
    '가로 넘침 자동 검사가 계속 "넘침 없음"으로 통과했는데, ' +
    '실제 스크린샷에서는 게임판이 오른쪽으로 잘려 있었다. ' +
    '검사와 눈으로 보는 것이 서로 다른 말을 하고 있었다.',
  attempt:
    'document.documentElement.scrollWidth > innerWidth 로 재고 있었는데, ' +
    'CSS에 html,body{overflow-x:hidden}이 걸려 있어 넘친 내용이 잘려나가므로 ' +
    'scrollWidth가 커지지 않는다. 어떤 경우에도 false를 반환하는 ' +
    '실패할 수 없는 검사였다. 게임판의 getBoundingClientRect().right를 ' +
    '뷰포트 경계와 직접 비교하도록 바꿨다.',
  comparison:
    '바꾼 검사로 11스테이지 전 구간을 재니 게임판이 16~374px(뷰포트 390px)에 ' +
    '있어 실제로는 넘치지 않았다. 잘못된 쪽은 스크린샷 촬영 도구였고, ' +
    '그 사실을 확인할 수 있게 된 것 자체가 검사를 고친 덕분이었다.',
  lesson:
    '초록불을 믿기 전에 그 검사가 빨간불이 될 수 있는 검사인지 먼저 확인해야 한다. ' +
    '항상 통과하는 검사는 검사가 아니라 장식이다.',
}
```

---

## 3. 붙여넣을 때 확인할 것

- [ ] `(배포 주소)`와 `(저장소 주소)`를 실제 주소로 바꿨는가
- [ ] 포트폴리오의 기존 강점 카드 개수를 확인하고, 새로 추가할지 기존 카드를 대체할지 정했는가
- [ ] 포트폴리오 데이터 파일의 실제 필드 이름과 위 예시의 필드 이름이 맞는지 확인했는가
- [ ] 게임 화면 스크린샷을 함께 넣을지 정했는가 (`prototype/shots/` 에 3상태 × 4해상도로 있음)
- [ ] **아직 배포 전이라는 점**을 감안했는가 — 배포 후에야 "3초 안에 플레이 시작" 같은 성능 수치를 채울 수 있다
