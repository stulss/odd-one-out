# 세션 인계 노트 — 2026-08-19 (2차 갱신)

> 다음 세션 시작 시 이 파일을 먼저 읽고 이어서 진행할 것.
> 1차 버전에서 "확인 필요"로 남겨뒀던 것들이 [세션_요약_2026-08-19.md](세션_요약_2026-08-19.md)
> (다른 세션의 작업 요약)로 대부분 해소됐다. 아래는 그 내용을 반영해 갱신한 버전이다.

## 프로젝트

- 이름: 「딱 하나 이상함」(Odd One Out) — 관찰력 미니게임, 학교 과제("내가 설계한 미니게임") 제출용
- 소스: https://github.com/stulss/odd-one-out
- **배포 주소 — 확정됨**: `https://odd-one-out-nine.vercel.app/` (Vercel)
  - 기존 `https://stulss.github.io/odd-one-out/` (GitHub Pages)에서 사용자 선택으로 **의도적 전환**.
  - 문서 6개 + 스크립트 9개 + 산출물(pptx/pdf) 전체에 일괄 반영 완료 (커밋 `7c78e2c`).
    소스코드 저장소 링크(github.com/stulss/odd-one-out)는 그대로 유지, 배포 주소만 교체됨.
  - → 더 이상 확인 불필요. 앞으로 새로 만드는 문서/스크립트도 이 주소를 써야 함.

## ✅ 직전 pending 작업 — 완료됨 (다른 세션에서 처리)

1차 인계 노트에서 미완료로 남겼던 **"발표자료.pdf에 검증안내서를 추가해줘"** 요청은
다른 세션에서 이미 처리 완료됨:

- "프로젝트 개요" 슬라이드 직후에 **기능 안내서 슬라이드**(목표·조작·진행·오늘의 문제·도전장·
  설정·기록·결과공유 8항목 표) 신규 삽입 → 20장 → 21장
- "17. 최종 체크리스트" 슬라이드 뒤, 스크린샷 증거 갤러리 앞에 **검증안내서.md 내용을 4장**으로
  분해 삽입:
  1. 개요(배포주소+3단계 방법) 2. 통과기준 1~5(표) 3. 통과기준 6~9 + 추가기능 3항목
     4. 안 될 때 FAQ 6항목(표)
  → 21장 → **최종 25장**
- `발표자료.pptx`/`발표자료.pdf` 재생성·재변환 완료, 새 배포주소(vercel) 반영 확인됨.
- 변환 중 `발표자료.pdf`가 ezPDF Editor에서 열려 있어 한 번 `발표자료_new.pdf`로 대체 저장되는
  사고가 있었으나, 사용자가 뷰어를 닫은 뒤 재변환해 정상적으로 `발표자료.pdf`에 덮어씀.

## 이번 세션(다른 세션)의 커밋 이력 (전부 `main`에 push 완료)

| 커밋 | 내용 |
|---|---|
| `fe7dc7e` | 발표자료에 기능 안내서 슬라이드 추가 |
| `b982764` | (merge) 원격의 `과제_제출_보고서.pdf` 추가 커밋과 병합 |
| `7c70e9b` | 발표자료에 검증 안내서 슬라이드 4장 추가 |
| `7c78e2c` | 공개 배포 주소를 Vercel로 변경 |

> 참고: 사용자가 GitHub 웹에서 `과제_제출_보고서.pdf`를 두 차례(`8b73ca7`, `21a6107`) 직접
> 추가/수정해서 로컬 작업과 병합하는 과정이 한 번 있었음. rebase는 `발표자료.pdf` 락 때문에
> 실패했고, merge 전략으로 전환해 해결됨. **교훈: 이 저장소는 사용자가 GitHub 웹 UI로도 직접
> 커밋하는 경우가 있으므로, 작업 시작 전 `git fetch && git log origin/main` 으로 원격에
> 새 커밋이 없는지 먼저 확인할 것.**

## 현재 확정 상태 (다른 세션 종료 시점)

- 작업 트리 clean, `origin/main`과 완전히 동기화됨.
- `발표자료.pdf` **25페이지**, 새 URL(vercel) 반영 확인 완료.
- `발표자료.pptx`/`.pdf`, 문서 6종, `tools/` 스크립트 9종 전부 배포주소 vercel로 통일됨.

## 다음 세션에서 확인/재점검하면 좋은 것 (필수는 아님)

- `작업내역_체크리스트.md`의 작업로그·D-14b 항목이 위 4개 커밋(기능안내서·검증안내서 4장·
  URL변경)까지 반영해 25장으로 갱신됐는지 — 다른 세션이 안 했다면 다음 세션에서 append.
- `tools/sim_difficulty.py`(난이도 T 비교 자동플레이) 결과가 발표자료나 프로젝트기록 문서에
  실제로 인용됐는지 — docs/06_프로젝트기록.md 5장에 T=3.6/4.4/5.4 비교표가 있었으니 아마 반영됨,
  확인만.
- `minigame_test/` 폴더는 odd-one-out과 별개의 초기 프로토타입 저장소로 보이며 이번 세션
  요약에도 언급 없음 — 특별히 손댈 필요 없음.
- 사용자가 최근 새 요청을 하면, 위 배포주소·25장 상태를 기준으로 이어갈 것 (구 20장/vercel
  이전 상태를 기준으로 되돌리지 말 것).

## 핵심 원칙 (계속 유지)

- 데이터 절대 조작 금지. AI/자동 생성 데이터는 반드시 명시적으로 라벨링
  (예: "자동플레이(사람아님)") — 사람이 플레이한 기록으로 제출 금지.
- 증거 스크린샷/슬라이드에 이모지 사용 금지 — PowerPoint→PDF 변환과 헤드리스 브라우저에서
  깨진 글리프(☑, 네모)로 렌더됨. 항상 텍스트로 대체.
- 문서 간 일관성 유지: README, 작업내역_체크리스트, 검증안내서, docs/*, 발표자료 전부
  같은 배포 주소(vercel)·같은 수치를 가리켜야 함.
- 커밋 메시지는 한국어로 설명적으로 작성, 끝에
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` (또는 실제 사용 모델명) 트레일러 포함.
- PPTX/PDF 변환 시 `POWERPNT.EXE` 잔여 프로세스·`~$*` 락파일·외부 뷰어(ezPDF 등)의 PDF 점유
  문제 상시 주의. 재시도+대체 파일명(`_new.pdf`) 로직이 `tools/pptx_to_pdf.py`에 이미 구현됨.
- **작업 시작 전 `git fetch`로 원격 변경 여부 확인** — 사용자가 GitHub 웹으로 직접 커밋하는
  경우가 실제로 있었음(이번에 `과제_제출_보고서.pdf` 사례).

## 반복되는 워크플로 패턴 (발표자료 수정 시)

1. `tools/build_slides.py` 수정
2. `python tools/build_slides.py` 로 pptx 재생성
3. `POWERPNT.EXE` 강제종료, `~$*` 락파일 정리, PDF를 열어둔 외부 뷰어가 있으면 닫아달라고 요청
4. `python tools/pptx_to_pdf.py` 로 pdf 변환 (락 시 자동 재시도 → 실패하면 `_new.pdf`)
5. pymupdf(fitz)로 페이지를 PNG로 렌더해 육안 확인 (poppler 없어서 Read 툴의 PDF 렌더 불가)
6. `README.md`, `작업내역_체크리스트.md` 동기화 (슬라이드 수, 작업로그 새 행 append)
7. `git fetch` 로 원격 변경 확인 → 필요시 merge → `git add -A && commit`(한국어+트레일러)
   `&& git push`
8. `SendUserFile`로 pptx/pdf 전달

## 참고 파일 위치

- 프로젝트 루트: `C:\Users\stuls\Desktop\developer stuls\odd-one-out\`
- 발표자료 생성기: `tools/build_slides.py` (현재 25장 구성)
- PDF 변환기: `tools/pptx_to_pdf.py`
- 증거 캡처 스크립트: `tools/shot_evidence.py`, `tools/verify_stability10min.py`,
  `tools/sim_difficulty.py`, `tools/shot_actions.py`, `tools/shot_difficulty.py`
- 검증 안내 원본: `검증안내서.md` (배포주소 vercel로 확정)
- 이번 세션 작업 요약(원본): [세션_요약_2026-08-19.md](세션_요약_2026-08-19.md)
